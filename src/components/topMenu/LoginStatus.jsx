import React, { Component, PropTypes } from "react";
import styles from "./TopMenuIndex.scss";

const LoginStatus = () => (
    <div className = {styles.LoginStatus}>Hello jim</div>
);

export default LoginStatus;