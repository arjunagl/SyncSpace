// import { incrementalSearchEpic } from './incrementalSearchEpic';
// import { SampleStores } from '../../../data/sampleData';

describe('Incremental Search Epic', () => {
    it('Should create an incremental search results', () => {
        //Currently there is no proper documented way to test epics, 
        // one option might be marble diagrams.        
         expect(true).toEqual(true);
    });
});
