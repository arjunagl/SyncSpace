describe('Incremental Search Mock', () => {
    it('Should return a set of mock search results', () => {
        //Currently there is no proper documented way to test redux observables, 
        // one option might be marble diagrams, however this is not clear yet.
         expect(true).toEqual(true);
    });
});
